# E106 프로젝트 코딩 컨벤션 가이드

## 1. 공통 규칙

### 1.1 케이스 규칙

| 케이스          | 형태        | 사용처                  |
| --------------- | ----------- | ----------------------- |
| **camelCase**   | `userName`  | 변수, 함수, 메서드      |
| **PascalCase**  | `UserName`  | 클래스, 컴포넌트        |
| **snake_case**  | `user_name` | DB 컬럼                 |
| **UPPER_SNAKE** | `USER_NAME` | 상수                    |
| **kebab-case**  | `user-name` | URL, CSS 클래스, 폴더명 |

---

## 2. Frontend 컨벤션 (Vue.js)

### 2.1 파일 네이밍

| 대상       | 컨벤션              | 예시              |
| ---------- | ------------------- | ----------------- |
| 컴포넌트   | PascalCase          | `CallPanel.vue`   |
| 페이지     | PascalCase + Page   | `LoginPage.vue`   |
| Composable | camelCase + use     | `useWebSocket.js` |
| Store      | camelCase + Store   | `authStore.js`    |
| Service    | camelCase + Service | `authService.js`  |
| 유틸리티   | camelCase           | `formatters.js`   |
| 폴더       | kebab-case          | `call-panel/`     |

### 2.2 컴포넌트 네이밍

| 유형            | 접두사/접미사 | 예시                         |
| --------------- | ------------- | ---------------------------- |
| 공통 컴포넌트   | Base-         | `BaseButton`, `BaseInput`    |
| 단일 인스턴스   | The-          | `TheHeader`, `TheSidebar`    |
| 페이지          | -Page         | `LoginPage`, `DashboardPage` |
| 도메인 컴포넌트 | 도메인명-     | `CallPanel`, `CustomerInfo`  |

### 2.3 변수 네이밍

```jsx
// ✅ 좋은 예
const userName = "John"; // 일반 변수: camelCase
const isLoading = false; // Boolean: is 접두사
const hasError = true; // Boolean: has 접두사
const canSubmit = true; // Boolean: can 접두사
const userList = []; // 배열: 복수형
const MAX_RETRY_COUNT = 3; // 상수: UPPER_SNAKE

// ❌ 나쁜 예
const user_name = "John"; // snake_case ❌
const loading = false; // is 접두사 없음 ❌
const data = []; // 불명확 ❌
```

### 2.4 함수 네이밍

| 접두사       | 용도          | 예시              |
| ------------ | ------------- | ----------------- |
| `get`        | 동기 값 반환  | `getFullName()`   |
| `fetch`      | API 조회      | `fetchUserList()` |
| `create`     | API 생성      | `createRoom()`    |
| `update`     | API 수정      | `updateProfile()` |
| `delete`     | API 삭제      | `deleteRecord()`  |
| `handle`     | 이벤트 핸들러 | `handleClick()`   |
| `validate`   | 유효성 검사   | `validateEmail()` |
| `format`     | 포맷 변환     | `formatTime()`    |
| `is/has/can` | Boolean 반환  | `isValid()`       |

### 2.5 Props & Emits

```jsx
// Props: camelCase, 명확한 타입
defineProps({
  userName: String, // ✅ camelCase
  isDisabled: Boolean, // ✅ is 접두사
  maxLength: Number,
  itemList: Array,
});

// Emits: 동사 또는 과거형
defineEmits([
  "submit", // ✅ 동사
  "cancel",
  "update:modelValue", // ✅ v-model용
  "callEnded", // ✅ 과거형
]);
```

### 2.6 Pinia Store

```jsx
export const useCallStore = defineStore("call", {
  // State: 명사형
  state: () => ({
    currentRoom: null,
    callStatus: "idle",
    isConnecting: false,
  }),

  // Getters: 명사형 또는 is 접두사
  getters: {
    isCallActive: (state) => state.callStatus === "connected",
    callDuration: (state) => {
      /* ... */
    },
  },

  // Actions: 동사형
  actions: {
    async startCall(roomId) {
      /* ... */
    },
    async endCall() {
      /* ... */
    },
    resetState() {
      /* ... */
    },
  },
});
```

---

## 3. Backend 컨벤션 (Spring Boot)

### 3.1 클래스 네이밍

| 유형          | 접미사      | 예시                |
| ------------- | ----------- | ------------------- |
| 컨트롤러      | Controller  | `AuthController`    |
| 서비스        | Service     | `AuthService`       |
| 서비스 구현체 | ServiceImpl | `AuthServiceImpl`   |
| 레포지토리    | Repository  | `AgentRepository`   |
| 엔티티        | (없음)      | `Agent`, `Customer` |
| DTO 요청      | Request     | `LoginRequest`      |
| DTO 응답      | Response    | `LoginResponse`     |
| 예외          | Exception   | `CustomException`   |
| 설정          | Config      | `SecurityConfig`    |
| 유틸리티      | Util        | `JwtUtil`           |

### 3.2 Controller 메서드

```java
@RestController
@RequestMapping("/api/auth")
public class AuthController {

    // 생성: create 또는 register
    @PostMapping("/register")
    public ApiResponse<RegisterResponse> register(@RequestBody RegisterRequest request) { }

    // 조회 (단건): get + 명사
    @GetMapping("/profile")
    public ApiResponse<AgentResponse> getProfile() { }

    // 조회 (목록): get + 복수형
    @GetMapping("/agents")
    public ApiResponse<List<AgentResponse>> getAgentList() { }

    // 수정: update + 명사
    @PutMapping("/profile")
    public ApiResponse<AgentResponse> updateProfile(@RequestBody UpdateRequest request) { }

    // 삭제: delete + 명사
    @DeleteMapping("/account")
    public ApiResponse<Void> deleteAccount() { }
}

```

### 3.3 Service 메서드

| 접두사       | 용도                 | 예시                       |
| ------------ | -------------------- | -------------------------- |
| `get`        | 단건 조회 (필수)     | `getAgentById()`           |
| `find`       | 단건 조회 (Optional) | `findAgentByEmail()`       |
| `get + List` | 목록 조회            | `getAgentList()`           |
| `create`     | 생성                 | `createRoom()`             |
| `register`   | 회원가입             | `register()`               |
| `update`     | 수정                 | `updateProfile()`          |
| `delete`     | 삭제                 | `deleteAccount()`          |
| `validate`   | 검증                 | `validateDuplicateEmail()` |
| `is/has/can` | Boolean 반환         | `isEmailAvailable()`       |

```java
@Service
public interface AuthService {

    // 조회 (필수값): get - 없으면 예외
    public Agent getAgentById(Long id) {
        return agentRepository.findById(id)
            .orElseThrow(() -> new CustomException(ErrorCode.AGENT_NOT_FOUND));
    }

    // 조회 (선택값): find - Optional 반환
    public Optional<Agent> findAgentByEmail(String email) {
        return agentRepository.findByEmail(email);
    }

    // 검증: validate
    private void validateDuplicateEmail(String email) {
        if (agentRepository.existsByEmail(email)) {
            throw new CustomException(ErrorCode.DUPLICATE_EMAIL);
        }
    }

    // Boolean: is/has/can
    public boolean isEmailAvailable(String email) {
        return !agentRepository.existsByEmail(email);
    }
}

```

### 3.4 Repository 메서드

```java
public interface AgentRepository extends JpaRepository<Agent, Long> {

    // 단건 조회: findBy + 필드명
    Optional<Agent> findByEmail(String email);

    // 목록 조회: findAllBy + 조건
    List<Agent> findAllByStatus(AgentStatus status);

    // 존재 여부: existsBy + 필드명
    boolean existsByEmail(String email);

    // 개수: countBy + 조건
    long countByStatus(AgentStatus status);

    // 삭제: deleteBy + 조건
    void deleteByEmail(String email);
}

```

### 3.5 Entity 규칙

```java
@Entity
@Table(name = "agents")
@Getter                                    // ✅ Getter만 사용
@NoArgsConstructor(access = AccessLevel.PROTECTED)  // ✅ 기본 생성자 protected
public class Agent {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String email;
    private String name;

    // 생성: @Builder 사용
    @Builder
    private Agent(String email, String name) {
        this.email = email;
        this.name = name;
    }

    // 수정: 비즈니스 메서드 (동사형)
    public void updateProfile(String name, String phone) {
        this.name = name;
        this.phone = phone;
    }

    public void changeStatus(AgentStatus status) {
        this.status = status;
    }
}

```

### 3.6 DTO 규칙

```java
// 요청 DTO: 접미사 Request
@Getter
@NoArgsConstructor
public class LoginRequest {
    private String email;
    private String password;
}

// 응답 DTO: 접미사 Response + 정적 팩토리 메서드
@Getter
@Builder
public class LoginResponse {
    private Long agentId;
    private String email;
    private String token;

    // 정적 팩토리: from 또는 of
    public static LoginResponse from(Agent agent, String token) {
        return LoginResponse.builder()
            .agentId(agent.getId())
            .email(agent.getEmail())
            .token(token)
            .build();
    }
}

```

### 3.7 DB 컬럼 네이밍

| Java 필드   | DB 컬럼      |
| ----------- | ------------ |
| `agentId`   | `agent_id`   |
| `createdAt` | `created_at` |
| `isDeleted` | `is_deleted` |
| `callCount` | `call_count` |

---

## 4. GitLab 브랜치 전략

### 4.1 브랜치 구조 예시

```
main (배포용 - 보호 브랜치)
  │
  ├── feat/S14P11E106-000-AUTH-S01        # 인증/회원 스토리
  ├── feat/S14P11E106-000-CUST-S02        # 고객 접수 스토리
  ├── feat/S14P11E106-000-QUEUE-S03       # 대기열/배정 스토리
  ├── feat/S14P11E106-000-CALL-S04        # 통화 연결 스토리
  ├── feat/S14P11E106-000-INFRA-T01       # 인프라 태스크
  ├── chore/server-init                   # 서버 시작
  ├── fix/S14P11E106-000-audio-quality    # 음질 개선 버그 수정
  ├── docs/api-specification              # API 명세서
  └── refactor/S14P11E106-000-stt-module  # 코드 구조 변경

```

### 4.2 브랜치 네이밍 규칙

| 브랜치 타입 | 네이밍 패턴                | 예시                                 | 용도                |
| ----------- | -------------------------- | ------------------------------------ | ------------------- |
| 기능 개발   | `feat/[JIRA-KEY]-스토리명` | `feat/S14P11E106-000-AUTH-S01`       | 새로운 기능 개발    |
| 유지 보수   | `chore/수정내용`           | `chore/server-init`                  | 단순 유지 보수 작업 |
| 버그 수정   | `fix/[JIRA-KEY]-버그설명`  | `fix/S14P11E106-000-audio-delay`     | 버그 수정           |
| 문서 작업   | `docs/문서명`              | `docs/api-spec`, `docs/readme`       | 문서 작성/수정      |
| 리팩토링    | `refactor/[JIRA-KEY]-대상` | `refactor/S14P11E106-000-stt-module` | 코드 리팩토링       |

**주의사항**

- 브랜치명에 JIRA 이슈 키를 반드시 포함 (자동 연동을 위함)
- 기능명은 kebab-case 사용 (소문자-하이픈)
- 한글/영어 혼용 가능하나 일관성 유지

---

### 4.3 작업 흐름

```jsx
# 1. develop에서 최신 코드 받기
$ git switch develop
$ git pull origin develop

# 2. JIRA 티켓 기반으로 feature 브랜치 생성
$ git switch -c feat/S14P11E106-000-AUTH-S01

# 3. 작업 후 커밋 (태스크 JIRA 키 포함)
$ git add .
$ git commit -m "[feat] S14P11E106-000 #done: WebRTC PeerConnection 초기 설정"

- RTCPeerConnection 객체 생성
- ICE candidate 핸들링 로직 추가
- Signaling 서버 연동 준비"

# 4. 태스크 별로 작업 후 원격 저장소 스토리 브랜치에 푸쉬
$ git push origin feature/S14P11E106-000-AUTH-S01
# 또는
$ git push

# 5. GitLab에서 Merge Request 생성
# - Source: feat/S14P11E106-000-AUTH-S01
# - Target: main
# - Assignee: 리뷰어 지정
# - Description에 JIRA 링크 자동 연동 확인

# 6. 코드 리뷰 완료 후 Merge

# 7. 로컬 브랜치 삭제
$ git switch develop
$ git branch -d feature/S14P11E106-000-AUTH-S01
```

---

### 4.4 브랜치 보호 규칙 (GitLab Settings)

**main 브랜치**

- ✅ Direct push 금지
- ✅ Merge 전 필수 승인: 리더/팀원 中 1명
- ✅ Pipeline 성공 필수
- ✅ 브랜치 삭제 금지

---

## 5. 커밋 메시지 컨벤션

### 5.1 커밋 메시지 구조

```
[<type>] <JIRA-KEY> <JIRA-STORY> <issue>: <subject>

<body>

<footer>
```

### 5.2 Type 종류

| Type       | 설명             | 예시                                                            | JIRA 상태 변경 |
| ---------- | ---------------- | --------------------------------------------------------------- | -------------- |
| `feat`     | 새로운 기능 추가 | `[feat] S14P11E106-000 CALL-S01 #done: 화상통화 연결 기능 구현` | 진행 중 → 완료 |
| `fix`      | 버그 수정        | `[fix] S14P11E106-000 VOICE-S02: 마이크 권한 오류 해결`         | -              |
| `docs`     | 문서 수정        | `[docs]: README 실행 방법 추가`                                 | -              |
| `style`    | 코드 포맷팅      | `[style] S14P11E106-000 USER-S01: ESLint 규칙 적용`             | -              |
| `refactor` | 코드 리팩토링    | `[refactor] S14P11E106-000 TEXT-S01: STT 모듈 구조 개선`        | -              |
| `test`     | 테스트 코드      | `[test] S14P11E106-000 CALL-S03: 통화 종료 시나리오 테스트`     | -              |
| `chore`    | 빌드/설정        | `[chore]: Webpack 설정 변경`                                    | -              |
| `perf`     | 성능 개선        | `[perf] S14P11E106-000 VOICE-S03: 음성 버퍼링 최적화`           | -              |

---

### 5.3 커밋 규칙

- ✅ 제목은 50자 이내 (한글 기준)
- ✅ 제목 끝에 마침표(.) 사용하지 않음
- ✅ 제목은 명령문으로 작성 ("추가했음" ❌ / "추가" ✅)
- ✅ 본문은 72자마다 줄바꿈
- ✅ 본문에 "무엇을", "왜" 변경했는지 설명
- ✅ 하나의 커밋 = 하나의 논리적 변경
- ✅ JIRA 이슈 키를 반드시 포함 (자동 연동)

---

## 6. JIRA 이슈 관리

### 6.1 JIRA 이슈 상태 (Workflow)

```
To Do → In Progress → Done
```

| 상태            | 설명      | 담당자 액션                 |
| --------------- | --------- | --------------------------- |
| **To Do**       | 작업 대기 | -                           |
| **In Progress** | 작업 중   | 브랜치 생성, 코드 작성 시작 |
| **Done**        | 완료      | develop에 머지 완료         |

---

### 6.2 우선순위 기준

| 우선순위    | 설명                             | 예시                   |
| ----------- | -------------------------------- | ---------------------- |
| **Highest** | 즉시 처리 필수 (프로젝트 블로커) | 배포 실패, 치명적 버그 |
| **High**    | 이번 스프린트 내 필수            | 핵심 기능 구현         |
| **Medium**  | 중요하지만 유연한 일정           | 부가 기능, UI 개선     |
| **Low**     | 여유 있을 때 처리                | 코드 정리, 문서 보완   |

---

## 7. GitLab ↔ JIRA 연동

**JIRA Smart Commits**

GitLab 커밋 메시지에 특정 키워드를 사용하면 JIRA 이슈가 자동 업데이트됩니다.

### 7.1 기본 문법

```
[<type>] <JIRA-KEY> <command> <optional-comment>: <subject>
```

---

## 7.2 주요 명령어

| 명령어            | 설명             | 예시                                    |
| ----------------- | ---------------- | --------------------------------------- |
| `#<상태>`         | 이슈 상태 전환   | `#in-progress`, `#done`                 |
| `#time <시간>`    | 작업 시간 기록   | `#time 2h`, `#time 30m`, `#time 1h 30m` |
| `#comment <내용>` | 이슈에 댓글 추가 | `#comment API 구현 완료`                |

> ⚠️ 상태 전환 명령어가 작동하지 않는 경우, JIRA 워크플로우 Transition 이름이 한글이거나 다를 수 있음. 이 경우 JIRA에서 직접 상태 변경 필요.

---

## 7.3 실전 예시

### 상태 전환

```bash
git commit -m "[feat] S14P11E106-000 #in-progress: 로그인 API 구현 시작"
git commit -m "[feat] S14P11E106-000 #done: 로그인 API 구현 완료"
```

### 시간 기록

```bash
git commit -m "[feat] S14P11E106-000 #time 2h: 로그인 API 구현"
git commit -m "[fix] S14P11E106-000 #time 30m: WebSocket 연결 버그 수정"
git commit -m "[feat] S14P11E106-000 #time 1h 30m: Google STT 연동"
```

### 댓글 추가

```bash
git commit -m "[feat] S14P11E106-000 #comment JWT 방식으로 구현: 로그인 기능"
git commit -m "[fix] S14P11E106-000 #comment 샘플링레이트 이슈 해결: 오디오 변환"
```

### 복합 사용

```bash
git commit -m "[feat] S14P11E106-000 #time 2h #in-progress: 로그인 API 구현 시작"
git commit -m "[feat] S14P11E106-000 #time 3h #done: 로그인 API 구현 완료"
git commit -m "[fix] S14P11E106-000 #time 1h #done #comment 재연결 로직 추가: 연결 끊김 버그 수정"
```

---

### 7.4 GitLab Merge Request → JIRA 연동

**MR 제목 규칙**

```
[type] JIRA-KEY JIRA-STORY Subject

예: [feat] S14P11E106-000 CALL-S01 WebRTC 1:1 통화 연결 기능 구현
```

**MR Description 템플릿**

```markdown
## 🎫 JIRA Issue

- Issue: [S14P11E106-000](https://your-jira.atlassian.net/browse/S14P11E106-000)
- Type: Story / Task
- Sprint: Sprint 1

## 📝 변경 사항

- WebRTC PeerConnection 초기 설정
- Signaling 서버 연동
- 1:1 통화 UI 컴포넌트 개발

## ✅ 체크리스트

- [x] 코드 작성 완료
- [x] 로컬 테스트 완료
- [x] 커밋 메시지 컨벤션 준수
- [ ] 코드 리뷰 완료
- [ ] 통합 테스트 완료

## 🧪 테스트 방법

1. `npm run dev` 실행
2. 브라우저에서 localhost:3000 접속
3. "통화 시작" 버튼 클릭
4. 다른 브라우저에서 접속하여 통화 연결 확인

## 📎 스크린샷

[스크린샷 첨부]

## 🔗 관련 MR

- Depends on: !12
- Related to: !15
```

---

### 7.5 MR 승인 규칙

| 브랜치         | 필수 승인 | 승인자                      |
| -------------- | --------- | --------------------------- |
| main ← feature | 1명       | 같은 파트 or 다른 파트 팀원 |

---

## 8. 코드 리뷰 프로세스

### 8.1 리뷰 요청 시 (Author)

1. ✅ Self-review 먼저 진행
2. ✅ MR Description 상세히 작성
3. ✅ 테스트 완료 확인
4. ✅ CI/CD 파이프라인 통과 확인
5. ✅ 리뷰어 지정 (JIRA Assignee와 다를 수 있음)

---

### 8.2 리뷰 진행 시 (Reviewer)

**체크 포인트**

- [ ] 코드 컨벤션 준수 (ESLint, Prettier)
- [ ] 명세서 요구사항 충족
- [ ] 성능 이슈 없음
- [ ] 보안 취약점 없음
- [ ] 테스트 코드 포함 (필요 시)
- [ ] 주석이 적절함
- [ ] 에러 핸들링 적절함

### 8.3 리뷰 코멘트 규칙

| 태그         | 의미        | 예시                                |
| ------------ | ----------- | ----------------------------------- |
| `[MUST]`     | 필수 수정   | `[MUST] null 체크 필요`             |
| `[SHOULD]`   | 권장 수정   | `[SHOULD] 변수명 더 명확하게`       |
| `[QUESTION]` | 질문        | `[QUESTION] 이 로직이 필요한 이유?` |
| `[NITPICK]`  | 사소한 의견 | `[NITPICK] 들여쓰기 수정`           |
| `[PRAISE]`   | 칭찬        | `[PRAISE] 깔끔한 구현!`             |

---

### 8.4 승인 기준

**✅ Approve (승인)**

- 모든 MUST 수정 완료
- 기능 동작 확인 완료
- 코드 품질 만족

**🔄 Request Changes (수정 요청)**

- MUST 수정사항 있음
- 기능 동작 불완전
- 명세서 요구사항 미충족

**💬 Comment (의견만)**

- 질문이나 제안만
- 승인/거부 아님

---

### 8.5 리뷰 후 (Author)

1. ✅ 리뷰 코멘트에 답변
2. ✅ MUST 항목 모두 수정
3. ✅ 수정 후 "Re-review 요청"
4. ✅ 승인 받으면 Merge
5. ✅ JIRA 이슈 상태 "Done"으로 변경
6. ✅ feature 브랜치 삭제

---

## 9. 코드 품질 관리

### 9.1 GitLab CI/CD 파이프라인

```yaml
# .gitlab-ci.yml
stages:
  - lint
  - test
  - build

lint:
  stage: lint
  script:
    - npm run lint
    - npm run format:check
  only:
    - merge_requests

test:
  stage: test
  script:
    - npm run test
    - npm run test:coverage
  coverage: '/Statements\s*:\s*([^%]+)/'
  only:
    - merge_requests

build:
  stage: build
  script:
    - npm run build
  only:
    - develop
    - main
```

---

### 9.2 필수 체크 항목

- ✅ ESLint: 0 errors
- ✅ Prettier: 포맷 통과
- ✅ Unit Test: 커버리지 80% 이상 (목표)
- ✅ Build: 성공
- ✅ No console.log (production)
- ✅ No TODO/FIXME (긴급 처리)

---

## 10. 긴급 상황 대응

### 10.1 Hotfix 프로세스

```bash
# 1. main에서 hotfix 브랜치 생성
git checkout main
git pull origin main
git checkout -b hotfix/WEBRTC-999-critical-bug

# 2. 긴급 수정
git commit -m "hotfix(WEBRTC-999): 치명적 버그 긴급 수정"

# 3. main과 develop 모두에 머지
git push origin hotfix/WEBRTC-999-critical-bug
# MR 생성: main ← hotfix
# MR 생성: develop ← hotfix

# 4. JIRA에서 Hotfix 라벨 추가

```

---

## 11. 프로젝트 메트릭 (주간 리뷰)

매주 회고에서 다음 메트릭 확인:

- 완료된 스토리 포인트 (Velocity)
- MR 평균 리뷰 시간
- 버그 발생 건수
- 코드 커버리지
- 기술 부채 이슈 수

---

## 12. 체크리스트 요약

### 작업 시작 전

- [ ] JIRA에서 이슈 확인 및 상태 "In Progress"로 변경
- [ ] develop에서 최신 코드 pull
- [ ] feature/[JIRA-KEY]-기능명 브랜치 생성

### 작업 중

- [ ] 커밋 메시지에 JIRA 키 포함
- [ ] 의미 있는 단위로 커밋
- [ ] 주기적으로 push (백업 목적)

### 작업 완료 시

- [ ] Self-review 진행
- [ ] 로컬 테스트 완료
- [ ] GitLab MR 생성 (description 작성)
- [ ] 리뷰어 지정 및 알림
- [ ] JIRA 이슈에 MR 링크 추가

### 리뷰 완료 후

- [ ] 승인 받으면 Squash merge
- [ ] feature 브랜치 삭제
- [ ] JIRA 이슈 "Done"으로 변경
- [ ] 팀에 완료 알림

---

## 13. MR Template

### 13.1 경로

`.gitlab/merge_request_templates/merge_request_default_template.md`

### 13.2 예시

```jsx
## 🎫 JIRA Issue
- Issue: [S14P11E106-000](https://your-jira.atlassian.net/browse/S14P11E106-000)
- Type: Story / Task
- Sprint: Sprint 1 / 2 / 3

## 📝 변경 사항
- (작업 내용을 요약해주세요)
- (...)

## ✅ 체크리스트
- [ ] 코드 작성 완료
- [ ] 로컬 테스트 완료
- [ ] 커밋 메시지 컨벤션 준수
- [ ] 코드 리뷰 완료
- [ ] 통합 테스트 완료

## 🧪 테스트 방법
1. (테스트 순서를 적어주세요)
2. (...)

## 📎 스크린샷
(필요 시 첨부)

## 🔗 관련 MR
- Depends on: !12
- Related to: !15

```

---

## 14. 참고 자료

- [GitLab Flow 공식 문서](https://docs.gitlab.com/ee/topics/gitlab_flow.html)
- [JIRA Smart Commits](https://support.atlassian.com/jira-software-cloud/docs/process-issues-with-smart-commits/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Code Review Best Practices](https://google.github.io/eng-practices/review/)
